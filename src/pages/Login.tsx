import React from 'react';
import { STYLES } from '../styles';

const Login = () => {
  return (
    <div
      className={`${STYLES.max_width} ${STYLES.margin_center} ${STYLES.paddingX} ${STYLES.paddingY} h-[100vh-]`}
    >
      login
    </div>
  );
};

export default Login;
